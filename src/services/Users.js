import axios from 'axios'

/**
 * Autentica un usuario con user + password
 * @param {*} user 
 * @param {*} password 
 */
export const authentication = (user, password) => {
    return Promise.resolve(()=>{return "peanut butter"})
}